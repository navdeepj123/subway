name: Deploy Express App to Apache Server

on:
  push:
    branches:
      - main  # Change this if using a different branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install SSH & SSHPass
        run: sudo apt-get install -y sshpass

      - name: Debug SSH Variables
        run: |
          echo "Server Host: ${{ secrets.95.111.218.189 }}"
          echo "Server Username: ${{ secrets.root }}"

      - name: Install Node.js Dependencies
        run: |
          npm install

      - name: Transfer Files to Server
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USERNAME: ${{ secrets.root }}
          SERVER_PASSWORD: ${{ secrets.xsp5pyryecz4p6p2 }}
        run: |
          sshpass -p "$SERVER_PASSWORD" scp -r * $SERVER_USERNAME@$SERVER_HOST:/var/www/express-app

      - name: Start Express App on Server
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USERNAME: ${{ secrets.root }}
          SERVER_PASSWORD: ${{ secrets.xsp5pyryecz4p6p2 }}
        run: |
          sshpass -p "$SERVER_PASSWORD" ssh -tt -o StrictHostKeyChecking=no $SERVER_USERNAME@$SERVER_HOST << 'EOF'
            cd /var/www/express-app
            npm install
            pm2 stop all
            pm2 start app.js --name "express-app"
            pm2 save
          EOF

